// SHOULD_PASS - Function with linear parameters
let square(x: field): field = 
    let x_copy = dup(x) in
    x_copy * x_copy

proof FunctionLinear {
    input a: field;
    let result = square(a) in
    result
}