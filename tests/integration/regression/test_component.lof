component Square {
    input x: field;
    witness y: field;

    {
        assert y === x * x;
        y
    }
}

proof UseSquare {
    input a: field;
    witness result: field;

    result === Square(a)
}
